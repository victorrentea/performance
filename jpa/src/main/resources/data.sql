INSERT INTO COUNTRY(ID, NAME)
SELECT 100+X, 'Country' || X
FROM SYSTEM_RANGE(1, 20);

INSERT INTO PARENT(ID, NAME, COUNTRY_ID, AGE)
SELECT 100 + X, 'Parent' || X || 'x', 1 + MOD(X, 20)+100, 20 + MOD(X * 17, 40),
FROM SYSTEM_RANGE(1, 1000);

INSERT INTO CHILD(ID, NAME, PARENT_ID)
SELECT 100+X, 'Child' || X || '-1', 100 + X
FROM SYSTEM_RANGE(1, 1000);

INSERT INTO CHILD(ID, NAME, PARENT_ID)
SELECT X + 1000+100, 'Child' || X || '-2', 100+X
FROM SYSTEM_RANGE(1, 1000);

